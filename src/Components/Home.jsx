import React from 'react';
import Feed from './Feed/Feed';

const Home = () => {
  return (
    <section className="container mainContainer">
      <Feed title="Photos" description="Dogs website home with photos feed." />
    </section>
  );
};

export default Home;
